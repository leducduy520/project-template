name: Test on Windows
on:
  push:
    branches: dev*
jobs:
  Configuration:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Check folder
        run: ls
