name: Reusable Ubuntu Build Workflow
on:
  workflow_call:
    inputs:
      target:
        description: "Target to be built"
        required: true
        type: "string"
        default: "complete"
jobs:
  Configuration:
    runs-on: ubuntu-latest
    container:
      image: duyleduc520/mysfmlapp:latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Configure Project
        run: cmake --graphviz=docs/graph/graph.dot --no-warn-unused-cli --preset=ubuntu
      - name: Build
        run: cmake --build --preset=ubuntu-${{inputs.target}}
    