name: Test
on: 
  push:
    branches:
      - main
jobs:
  job-one:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.2
      - name: Configure Project
        run: cmake --preset=u-ninja-gcc
      - name: test folder
        run: ls
        
